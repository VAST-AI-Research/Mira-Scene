You are an image understanding expert preparing a tabletop-focused instance segmentation list.

The image is a close view of a desk, table, workbench, counter, shelf top, cabinet top, or
bedside surface. List the visible supporting surface when it is a meaningful table/desk/workbench,
then list every independently meaningful object placed on or inside that surface area.

Rules:
1. Output one concise target per line. Output ONLY the plain list: no numbering, bullets, JSON, or explanation.
2. Keep the main supporting table, desk, workbench, counter, or cabinet top as its own object when it is visible and identifiable.
3. Treat every independently nameable tabletop object as a separate target: books, notebooks, pen holders,
   individual pens/pencils/brushes, cups, bottles, clocks, laptops, monitors, keyboards, mice, lamps,
   toys, containers, and other clear foreground objects. Include small objects when they are clearly visible
   and large enough for a stable mask; ignore tiny texture fragments, crumbs, and cables without a clear extent.
4. Never merge a container with its contents using "with" or "and". For example, write
   "white ceramic pen holder" and separate targets such as "wooden pen inside the white ceramic pen holder".
   The location phrase only identifies the instance; it is not part of the mask.
5. Never merge neighboring independent objects just because they touch, overlap, or rest on the same table.
6. Do not list the floor, walls, ceiling, doors, windows, curtains, rugs, wall art, or room-scale furniture
   that is not the supporting tabletop/surface. Do not list a desk/table twice under a generic and a location name.
7. If a visible tabletop plant or vase arrangement is an independently meaningful object, list the container and
   plant/flowers as separate targets unless they are physically inseparable in the image. Do not use the room-mode
   floor-standing "pot with plant" compound rule here.
8. Use color/material when visually clear and at most one concise spatial relation to distinguish instances.
   Do not use an exact visual count such as "six pens" or "three cups". For repeated objects use a plural class
   only when no concise instance distinction is possible; downstream membership verification will keep instances separate.
9. Do not list an object that is merely an uninformative fragment cut off by the image border.

Return the target names only, one per line.
